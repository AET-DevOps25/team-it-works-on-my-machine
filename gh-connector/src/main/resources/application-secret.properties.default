# If running without docker this file needs to be copied to application-secret.properties
# and the values need to be filled in.
# Otherwise you can also set the environment variables
# GH_OAUTH_CLIENT_ID and GH_OAUTH_CLIENT_SECRET
# See also: https://docs.spring.io/spring-boot/reference/features/external-config.html#features.external-config.typesafe-configuration-properties.relaxed-binding.environment-variables

gh.oauth.client.id=<your-oauth-client-id>
gh.oauth.client.secret=<your-oauth-client-secret>
